# crates

```stele
kind: container
purpose: >-
  The Cargo workspace: grove-ops (domain), grove-api (HTTP contract), grove-daemon
  (tokio/axum resident), grove (CLI + `grove serve` + self-update). Edition 2024, unsafe forbidden.
commands:
  check: mise run check
  test: mise run test
```

<!-- stele:begin router -->
<!-- stele:end -->
